import { AssignmentDirective } from './assignment.directive';

describe('AssignmentDirective', () => {
  it('should create an instance', () => {
    const directive = new AssignmentDirective();
    expect(directive).toBeTruthy();
  });
});
