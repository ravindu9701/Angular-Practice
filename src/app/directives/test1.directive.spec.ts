import { Test1Directive } from './test1.directive';

describe('Test1Directive', () => {
  it('should create an instance', () => {
    const directive = new Test1Directive();
    expect(directive).toBeTruthy();
  });
});
