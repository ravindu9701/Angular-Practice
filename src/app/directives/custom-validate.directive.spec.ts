import { CustomValidateDirective } from './custom-validate.directive';

describe('CustomValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomValidateDirective();
    expect(directive).toBeTruthy();
  });
});
