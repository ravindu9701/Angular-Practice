import { ChangeDetectionTestDirective } from './change-detection-test.directive';

describe('ChangeDetectionTestDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeDetectionTestDirective();
    expect(directive).toBeTruthy();
  });
});
